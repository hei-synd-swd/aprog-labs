##################################################
# Human vs. Zombie - OOP Lab
#

set shell := ["bash", "-uc"]
part := "part1"

# List all commands
default:
  just --list

# Run the game
@run:
  cargo run

# Run all tests with part selection [part1|part2] (single-threaded so output stays grouped by module)
@test part=part:
  cargo test --features="{{part}}" -- --test-threads=1

# Run tests, showing only failures
@test-quiet part=part:
  cargo test --features="{{part}}" 2>&1 | grep -E "(test |FAILED|error|panic)"

# Check that the project compiles
@check:
  cargo check

# Format code
@fmt:
  cargo fmt

# Run clippy
@lint:
  cargo clippy
