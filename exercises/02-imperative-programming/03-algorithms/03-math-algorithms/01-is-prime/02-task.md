---
id          = "impp_is_prime"
name        = "Is Prime"
language    = "rust"
difficulty  = 2
description = "Check if a number is prime."
topics      = ["loops", "math"]
---

# Is Prime

Write `is_prime(n: u64) -> bool` that returns `true` if `n` is prime.

Expected Result:
```
is_prime(2)   → true
is_prime(17)  → true
is_prime(1)   → false
is_prime(4)   → false
```
